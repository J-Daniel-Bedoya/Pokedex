import { createSlice } from '@reduxjs/toolkit';
  
export const colorBtnSlice = createSlice({
  name: '',
  initialState: "",
  reducers: {
  
  }
})
  
export const {  } = colorBtnSlice.actions;
export default colorBtnSlice.reducer;